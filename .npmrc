# primeng@13's manifest only declares peer support for Angular ^13; it works
# fine at runtime through Angular 20 (verified manually), but npm's strict
# peer resolution won't install it alongside newer @angular/* packages
# without this.
legacy-peer-deps=true
